interface:
  display_name: "GPT-5.6 Model Router"
  short_description: "Route with Sol design and creative freedom"
  default_prompt: "Use $gpt56-model-router to choose a profile, keep meaningful design on GPT-5.6 Sol, preserve creative exploration and Terra implementation latitude, and propose the adaptive route for approval."

policy:
  allow_implicit_invocation: false
